.DS_Store
.build/
.swiftpm/
Package.resolved
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
.claude/worktrees
xcuserdata/
.env
key.*

# Local secrets — never commit
Secrets.plist
.env

# Scratch space for development artifacts not checked in
tmp/

# .app bundle output from scripts/build-app.sh
build/

# Training data — too large to commit. README.md inside is committed separately.
training-data/*/
!training-data/README.md
